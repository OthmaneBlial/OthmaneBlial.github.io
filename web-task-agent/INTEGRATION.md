# Site Notes

This folder is a self-contained static site built from the repository state.

The root `docs/` folder is intended to mirror this `site/` folder so the same static content can be served from either location.

The public UI intentionally focuses on:

- what the project does
- how to run it
- what it produces
- where to inspect jobs and evidence

It does not surface deployment or hosting mechanics in the public pages.
